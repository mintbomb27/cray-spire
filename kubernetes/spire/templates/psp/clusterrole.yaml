apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "spire.fullname" . }}
rules:
- apiGroups:
  - policy
  resourceNames:
  - {{ include "spire.fullname" . }}
  resources:
  - podsecuritypolicies
  verbs:
  - use
