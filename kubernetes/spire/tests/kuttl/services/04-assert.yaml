apiVersion: v1
kind: Service
metadata:
  annotations:
    meta.helm.sh/release-name: spire
    meta.helm.sh/release-namespace: spire
  name: spire-postgres-0
  namespace: spire
spec:
  ports:
    - port: 5432
      protocol: TCP
      targetPort: 5432
  sessionAffinity: None
  type: ClusterIP
