apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "spire.fullname" . }}-agent
  labels:
    app.kubernetes.io/name: {{ template "spire.fullname" . }}-agent
    {{- include "spire.common-labels" . | nindent 4 }}
